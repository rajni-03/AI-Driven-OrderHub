@page "/"
@inject HttpClient Http

<h3>Ordering System</h3>

<button @onclick="CallServer">Call Server API</button>

<p>@message</p>

@code {
    private string message = "";

    private async Task CallServer()
    {
        message = await Http.GetStringAsync("api/test");
    }
}
